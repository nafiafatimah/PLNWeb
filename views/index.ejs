<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beranda - Invoice WE</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
        }
        .bg {
            background-image: url('/img/bg1.jpg');
            height: 100%; 
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            position: relative;
        }
        .overlay {
            background-color: rgba(0, 0, 0, 0.6);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }
        .main-content {
            position: relative;
            z-index: 2;
            color: white;
            text-align: center;
            padding: 20px; /* Padding di sini untuk ruang di sekitar konten */
            height: 100%; /* Mengisi tinggi penuh */
            display: flex; /* Menggunakan Flexbox */
            flex-direction: column; /* Membuat konten berorientasi vertikal */
            justify-content: center; /* Memusatkan konten secara vertikal */
            align-items: center; /* Memusatkan konten secara horizontal */
        }
        .navbar-custom {
            background-color: #007bff;
        }
        .btn-lg-custom {
            width: 200px;
            font-size: 1.25rem;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <!-- <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
            <a class="navbar-brand text-white" href="/">
                <img src="/img/logopln.png" alt="PLN Logo" style="width: 30px; height: auto; margin-right: 10px;"> Invoice WE
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/auth/register">Register</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav> -->

    <!-- Latar belakang dengan overlay -->
    <div class="bg">
        <div class="overlay"></div>
        <div class="main-content">
            <h1 class="display-4">Welcome to Invoice WEB</h1>
            <p class="lead">Manage your invoices easily and effectively with our platform.</p>
            <div class="mt-5">
                <a href="/auth/login" class="btn btn-primary btn-lg btn-lg-custom">Login</a>
                <a href="/auth/register" class="btn btn-secondary btn-lg btn-lg-custom">Register</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
